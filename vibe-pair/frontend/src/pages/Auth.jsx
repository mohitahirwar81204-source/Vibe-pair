import{useState}from"react";import{supabase}from"../lib/supabase";import Logo from"../components/Logo";
export default function Auth(){const[mode,setMode]=useState("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setError("");const r=mode==="login"?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password});if(r.error)setError(r.error.message);setLoading(false)}return <div className="flex min-h-[85vh] items-center justify-center"><div className="glass w-full max-w-md rounded-3xl p-8 shadow-2xl"><Logo/><h1 className="mt-8 text-3xl font-bold">{mode==="login"?"Find your vibe.":"Join Vibe Pair."}</h1><p className="mt-2 text-slate-400">{mode==="login"?"Meet people who match your energy.":"Create your profile and start connecting."}</p><form onSubmit={submit} className="mt-7 space-y-4"><input className="w-full rounded-2xl border border-white/10 bg-white/5 p-4 outline-none focus:border-vibe-pink" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="w-full rounded-2xl border border-white/10 bg-white/5 p-4 outline-none focus:border-vibe-pink" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required minLength="6"/><button disabled={loading} className="w-full rounded-2xl bg-gradient-to-r from-vibe-pink to-vibe-purple p-4 font-bold">{loading?"Please wait...":mode==="login"?"Enter Vibe Pair":"Create account"}</button></form>{error&&<p className="mt-4 text-sm text-red-300">{error}</p>}<button className="mt-6 text-sm text-slate-300 underline" onClick={()=>setMode(mode==="login"?"signup":"login")}>{mode==="login"?"New here? Create an account":"Already have an account? Login"}</button></div></div>}