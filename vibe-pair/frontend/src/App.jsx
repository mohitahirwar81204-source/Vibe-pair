import{useEffect,useState}from"react";import{Routes,Route,Link,useNavigate,useLocation}from"react-router-dom";import{supabase}from"./lib/supabase";import Auth from"./pages/Auth";import Discover from"./pages/Discover";import Chat from"./pages/Chat";import Profile from"./pages/Profile";import Matches from"./pages/Matches";
function Layout({user,children}){const nav=useNavigate();const loc=useLocation();async function logout(){await supabase.auth.signOut();nav("/login")}return <div className="min-h-screen bg-[#070712] text-white"><header className="sticky top-0 z-20 border-b border-white/10 bg-[#070712]/90 backdrop-blur"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><Link to="/" className="text-2xl font-black tracking-tight"><span className="text-vibe-pink">Vibe</span> Pair</Link>{user&&<nav className="hidden gap-5 text-sm text-slate-300 sm:flex"><Link className={loc.pathname==="/"?"text-white":""} to="/">Discover</Link><Link to="/matches">Matches</Link><Link to="/chat">Chat</Link><Link to="/profile">Profile</Link><button onClick={logout} className="text-pink-300">Logout</button></nav>}{user&&<button onClick={logout} className="sm:hidden text-pink-300">Logout</button>}</div></header><main className="mx-auto max-w-6xl px-5 py-8">{children}</main></div>}
export default function App(){const[user,setUser]=useState(null);useEffect(()=>{supabase.auth.getUser().then(({data})=>setUser(data.user));const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user??null));return()=>l.subscription.unsubscribe()},[]);return user?<Layout user={user}><Routes><Route path="/" element={<Discover/>}/><Route path="/matches" element={<Matches/>}/><Route path="/chat" element={<Chat/>}/><Route path="/profile" element={<Profile/>}/></Routes></Layout>:<Routes><Route path="*" element={<Auth/>}/></Routes>}